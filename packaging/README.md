# Packaging

Phase 8 produces one hyphae binary per supported platform, release archives,
checksums, SPDX and CycloneDX SBOMs, signatures, and build provenance.

Packaging definitions must not bundle a database, cache, model, or provider
credential.
