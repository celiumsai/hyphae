# Framework host smoke tests

These minimal Astro, Next, and Vite applications install only their host
framework dependencies. A successful `npm test` proves that each host builds
without the optional data-engine SDK or adapter package present.
