# Executable examples

Examples are added with each implemented public capability. They must run in
CI from a clean data directory and may not require network access unless the
example explicitly demonstrates a configured optional provider.
